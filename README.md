# Laravel eCommerce 플랫폼

커뮤니티 기능, 마케팅 리워드 시스템, 결제 시스템, 자산관리 시스템이 통합된 종합 Laravel 기반 eCommerce 플랫폼입니다.

## 주요 기능

### 🛒 eCommerce 시스템
- **상품 관리**: 카테고리가 포함된 상품 CRUD 기능
- **장바구니**: 실시간 재고 관리와 함께 상품 추가, 수정, 삭제
- **주문 관리**: 주문 생성부터 배송까지 전체 주문 생명주기 관리
- **재고 추적**: 구매 및 취소 시 자동 재고 업데이트

### 👥 커뮤니티 시스템
- **게시글 & 댓글**: 커뮤니티 콘텐츠 생성, 수정, 관리
- **소셜 상호작용**: 게시글 및 댓글 좋아요
- **사용자 프로필**: 팔로우/언팔로우 기능
- **참여도 추적**: 조회수, 좋아요, 댓글 수 추적

### 🎁 리워드 시스템
- **구매 포인트 적립**: 모든 구매에서 포인트 획득
- **추천인 프로그램**: 친구 추천 시 보너스 포인트 지급
- **리워드 교환**: 포인트로 할인, 상품, 바우처 교환
- **포인트 이력**: 모든 포인트 거래 내역 추적
- **만료 관리**: 자동 포인트 만료 처리

### 💳 결제 시스템
- **다양한 결제 수단**:
  - 신용카드
  - PayPal
  - Stripe
  - 지갑 (내부 잔액)
- **결제 추적**: 완전한 거래 내역
- **환불 관리**: 환불 요청 및 처리
- **지갑 시스템**: 충전 기능이 있는 내부 지갑

### 💰 자산관리 시스템
- **다양한 자산 유형**:
  - 현금: 현금 또는 은행 계좌
  - 카드: 신용/체크 카드 결제
  - 페이: 충전/승인 방식의 내부 페이 시스템
  - 리워드 포인트: 활동으로 획득한 포인트
- **자산 잔액 추적**: 각 자산 유형별 실시간 잔액
- **거래 내역**: 모든 자산 이동에 대한 완전한 감사 추적
- **페이 충전 시스템**:
  - 사용자 충전 요청
  - 관리자 승인 워크플로우
  - 자동 잔액 업데이트
- **자산 이체**: 자산 유형 간 이체
- **결제 통합**: 주문 결제에 자산 사용

## 데이터베이스 스키마

### 핵심 테이블
- **users**: 리워드 포인트와 지갑 잔액이 있는 사용자 계정
- **categories**: 상품 및 게시글 카테고리 (계층 구조)
- **products**: 가격 및 재고가 있는 상품 카탈로그
- **carts**: 장바구니 관리
- **orders**: 주문 처리 및 추적

### 커뮤니티 테이블
- **posts**: 사용자 생성 콘텐츠
- **comments**: 게시글에 대한 중첩 댓글
- **likes**: 게시글 및 댓글에 대한 다형성 좋아요
- **follows**: 사용자 팔로우 관계

### 리워드 테이블
- **rewards**: 교환 가능한 리워드
- **reward_redemptions**: 사용자 리워드 교환 이력
- **reward_points_transactions**: 완전한 포인트 원장
- **referrals**: 추천 추적 및 보너스 관리

### 결제 테이블
- **payments**: 결제 거래
- **refunds**: 환불 요청 및 처리
- **wallet_transactions**: 지갑 잔액 이력

### 자산관리 테이블
- **asset_types**: 자산 유형 (현금, 카드, 페이, 리워드 포인트)
- **user_assets**: 각 자산 유형별 사용자 잔액
- **asset_transactions**: 완전한 자산 거래 이력
- **pay_charge_requests**: 승인 워크플로우가 있는 페이 충전 요청

## API 엔드포인트

### 공개 엔드포인트

```
GET  /api/products              - 모든 상품 목록
GET  /api/products/{id}         - 상품 상세 정보
GET  /api/posts                 - 모든 게시글 목록
GET  /api/posts/{id}            - 게시글 상세 정보
GET  /api/rewards               - 이용 가능한 리워드 목록
GET  /api/users/{id}            - 사용자 프로필
```

### eCommerce 엔드포인트

```
# 장바구니 관리
GET    /api/cart                - 현재 장바구니 조회
POST   /api/cart/items          - 장바구니에 상품 추가
PUT    /api/cart/items/{id}     - 장바구니 상품 수정
DELETE /api/cart/items/{id}     - 장바구니 상품 삭제
DELETE /api/cart/clear          - 장바구니 비우기

# 주문
GET    /api/orders              - 사용자 주문 목록
GET    /api/orders/{id}         - 주문 상세 정보
POST   /api/orders              - 새 주문 생성
POST   /api/orders/{id}/cancel  - 주문 취소
```

### 커뮤니티 엔드포인트

```
# 게시글
POST   /api/posts               - 새 게시글 작성
PUT    /api/posts/{id}          - 게시글 수정
DELETE /api/posts/{id}          - 게시글 삭제
POST   /api/posts/{id}/like     - 게시글 좋아요/취소

# 댓글
GET    /api/posts/{postId}/comments    - 댓글 목록
POST   /api/posts/{postId}/comments    - 댓글 추가
PUT    /api/comments/{id}              - 댓글 수정
DELETE /api/comments/{id}              - 댓글 삭제
POST   /api/comments/{id}/like         - 댓글 좋아요/취소

# 사용자 관계
POST   /api/users/{id}/follow   - 팔로우/언팔로우
GET    /api/users/{id}/followers - 팔로워 목록
GET    /api/users/{id}/following - 팔로잉 목록
```

### 리워드 엔드포인트

```
POST   /api/rewards/{id}/redeem        - 리워드 교환
GET    /api/rewards/redemptions        - 내 교환 이력
GET    /api/rewards/points/history     - 포인트 이력

# 추천
GET    /api/referrals/code             - 내 추천 코드
GET    /api/referrals                  - 내 추천 목록
POST   /api/referrals/complete         - 추천 완료
```

### 결제 엔드포인트

```
# 결제
GET    /api/payments               - 내 결제 목록
GET    /api/payments/{id}          - 결제 상세 정보
POST   /api/payments               - 결제 생성
POST   /api/payments/{id}/confirm  - 결제 확인

# 환불
GET    /api/refunds                - 내 환불 목록
POST   /api/refunds                - 환불 요청
POST   /api/refunds/{id}/process   - 환불 처리 (관리자)

# 지갑
GET    /api/wallet/balance         - 지갑 잔액 조회
GET    /api/wallet/transactions    - 지갑 거래 이력
POST   /api/wallet/add-funds       - 지갑 충전
```

### 자산관리 엔드포인트

```
# 자산 유형 & 잔액
GET    /api/assets/types                        - 모든 자산 유형 목록
GET    /api/assets                              - 사용자의 모든 자산
GET    /api/assets/{assetTypeCode}/balance      - 특정 자산 잔액
GET    /api/assets/transactions                 - 모든 자산 거래 내역
GET    /api/assets/{assetTypeCode}/transactions - 유형별 거래 내역
POST   /api/assets/transfer                     - 자산 간 이체

# 페이 충전 요청 (사용자)
POST   /api/pay/charge-requests                 - 충전 요청 생성
GET    /api/pay/charge-requests                 - 내 충전 요청 목록
GET    /api/pay/charge-requests/{id}            - 충전 요청 상세 정보
POST   /api/pay/charge-requests/{id}/cancel     - 충전 요청 취소

# 페이 충전 요청 (관리자)
POST   /api/pay/charge-requests/{id}/approve    - 충전 요청 승인
POST   /api/pay/charge-requests/{id}/reject     - 충전 요청 거절
GET    /api/pay/charge-requests/admin/pending   - 대기 중인 요청 목록
GET    /api/pay/charge-requests/admin/statistics - 충전 통계
```

## 설치

### 필수 요구사항
- PHP 8.1 이상
- Composer
- MySQL 5.7+ 또는 PostgreSQL
- Node.js & NPM (프론트엔드 에셋 컴파일용)

### 설치 단계

1. **저장소 복제**
   ```bash
   git clone <repository-url>
   cd laravel-ecommerce-platform
   ```

2. **의존성 설치**
   ```bash
   composer install
   npm install
   ```

3. **환경 설정**
   ```bash
   cp .env.example .env
   php artisan key:generate
   ```

4. **데이터베이스 설정**
   `.env` 파일 수정:
   ```env
   DB_CONNECTION=mysql
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_DATABASE=laravel_ecommerce
   DB_USERNAME=root
   DB_PASSWORD=
   ```

5. **결제 게이트웨이 설정**
   `.env` 파일 수정:
   ```env
   STRIPE_KEY=your_stripe_key
   STRIPE_SECRET=your_stripe_secret
   PAYPAL_CLIENT_ID=your_paypal_client_id
   PAYPAL_SECRET=your_paypal_secret
   ```

6. **마이그레이션 실행**
   ```bash
   php artisan migrate
   ```

7. **데이터베이스 시딩 (선택사항)**
   ```bash
   php artisan db:seed
   ```

8. **개발 서버 시작**
   ```bash
   php artisan serve
   ```

애플리케이션은 `http://localhost:8000`에서 사용할 수 있습니다.

## 설정

### 리워드 시스템

`.env`에서 리워드 설정:
```env
REWARD_POINTS_PER_PURCHASE=10
REFERRAL_BONUS_POINTS=100
POINTS_TO_CURRENCY_RATIO=0.01
```

### 결제 시스템

`.env`에서 결제 설정:
```env
PAYMENT_GATEWAY=stripe
PAYMENT_CURRENCY=USD
TAX_RATE=0.10
SHIPPING_COST=10.00
```

### 자산관리 시스템

`.env`에서 자산 설정:
```env
ASSET_CURRENCY=USD
PAY_MIN_CHARGE_AMOUNT=1000
PAY_MAX_CHARGE_AMOUNT=1000000
PAY_AUTO_APPROVAL=false
PAY_AUTO_APPROVAL_LIMIT=10000
ASSET_DAILY_LIMIT=1000000
ASSET_MONTHLY_LIMIT=10000000
```

마이그레이션 실행 후, 기본 자산 유형 시딩:
```bash
php artisan db:seed --class=AssetTypeSeeder
```

## 인증

API는 Laravel Sanctum을 사용하여 인증합니다. 요청 헤더에 인증 토큰을 포함하세요:

```
Authorization: Bearer {token}
```

## 비즈니스 로직

### 주문 흐름
1. 사용자가 장바구니에 상품 추가
2. 장바구니에서 주문 생성
3. 장바구니가 비워지고 주문이 "pending" 상태로 생성됨
4. 사용자가 결제 시작
5. 결제 성공 시 주문 상태가 "processing"으로 변경
6. 사용자에게 리워드 포인트 지급
7. 상품 재고가 자동으로 감소

### 리워드 포인트
- 구매 금액의 10%를 포인트로 획득
- 1 포인트 = $0.01
- 포인트로 리워드 교환 가능
- 추천 보너스: 성공적인 추천당 100 포인트

### 지갑 시스템
- 잔액 저장을 위한 내부 지갑
- 구매에 사용 가능
- 환불 시 자동으로 지갑에 입금
- 결제 게이트웨이를 통한 충전 가능

### 자산관리 흐름
1. 시스템에 4가지 자산 유형 존재: 현금, 카드, 페이, 리워드 포인트
2. 각 사용자는 자산 유형별로 별도의 잔액 보유
3. 사용자는 모든 자산 잔액과 거래 이력 조회 가능
4. 주문 결제에 자산 사용 가능 (asset_type_code 선택)

### 페이 충전 흐름
1. 사용자가 금액과 결제 증빙으로 충전 요청 생성
2. 요청 상태가 "pending"으로 설정됨
3. 관리자가 충전 요청 검토
4. 관리자가 요청 승인 또는 거절
5. 승인 시:
   - 사용자의 페이 자산 잔액 증가
   - 자산 거래 기록
   - 충전 요청이 "approved"로 표시
6. 사용자가 페이 잔액으로 구매 가능

### 자산으로 결제
1. 결제 생성 시 `asset_type_code` 지정 (cash, card, pay)
2. "Pay" 유형의 경우:
   - 시스템이 충분한 페이 잔액 확인
   - 페이 자산에서 금액 차감
   - 자산 거래 기록
   - 결제 완료
3. "Cash"와 "Card"의 경우:
   - 잔액 확인 없이 직접 결제
   - 결제 거래 기록
4. 주문 상태가 "processing"으로 변경
5. 리워드 포인트가 자동으로 지급

## 개발

### 주요 관계

**User 모델** 연결:
- Orders (일대다)
- Cart (일대일)
- Posts (일대다)
- Referrals (일대다)
- Payments (일대다)
- Reward Points (일대다)

**Product 모델** 연결:
- Category (소속)
- Cart Items (일대다)
- Order Items (일대다)

**Order 모델** 연결:
- User (소속)
- Order Items (일대다)
- Payments (일대다)
- Refunds (일대다)

## 라이선스

이 프로젝트는 MIT 라이선스로 오픈소스 소프트웨어입니다.

## 지원

문제나 질문이 있으시면 GitHub에서 이슈를 열어주세요.

## 기여

기여를 환영합니다! Pull Request를 자유롭게 제출해주세요.
